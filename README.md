# Advanced RAG (FAISS + LM Studio + Metadata + Token Control)

Этот проект — продвинутый пример Retrieval-Augmented Generation (RAG), использующий:

- **FAISS** — быстрый векторный поиск
- **Nomic Embeddings** (через LM Studio)
- **Mistral-7B-Instruct** (или другую LLM, подключённую через LM Studio)
- **Фильтрацию по метаданным**
- **Ограничение токенов**
- **Переформулировку вопроса для улучшения поиска**
- **Автоматическую сборку контекста с указанием источников**

Файл: `advanced_rag.py`  
Доп. файлы:

- `chunks.jsonl` — подготовленные чанки
- `embeddings.npz` — вычисленные эмбеддинги

---

## 🚀 Установка

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```
